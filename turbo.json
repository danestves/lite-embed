{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "test": {},
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["**/dist/**"]
    },
    "prepublish": {
      "outputs": ["**/dist/**"]
    },
    "lint": {},
    "format": {}
  }
}
